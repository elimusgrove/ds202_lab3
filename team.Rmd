---
title: "team"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
# Load data
data = readxl::read_xls('data.xls')

# Library
library(ggplot2)

names(data) = c('year', 'understanding', 'happiness', 'political_party', 'region', 'total_income', 'race', 'sex', 'num_children', 'spouse_hours_work_week', 'marital_status', 'respondent_id', 'ballot_used')
```

### 1.
```{r}
ggplot(gss, aes(x=marital_status, fill=happiness)) + geom_bar()
```
